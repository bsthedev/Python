from tkinter import *

root = Tk()

for r in range(3):
   for c in range(4):
      Label(root, text = ('Row',r,'/Col',c),
            borderwidth = 2).grid(row = r,column = c)   #grid function uses rows and columns
      
root.mainloop()
